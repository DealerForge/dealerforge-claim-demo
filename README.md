# DealerForge CLAIM - Working Demo

Warranty Audit Protection and Supported Recovery Intelligence. Advisory only. Gate A (synthetic data). The system recommends; a human decides and acts; the OEM determines.

Run locally: pip install -r requirements.txt && streamlit run streamlit_app.py
